{
  "name": "translationadmy",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Website des Dolmetscherbüros Translation Admy, Bingen am Rhein",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build",
    "build:only": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "astro check && astro build && vitest run",
    "smtp:check": "node scripts/check-smtp.mjs",
    "smtp:test": "node scripts/check-smtp.mjs --send"
  },
  "dependencies": {
    "@astrojs/node": "^9.5.5",
    "@astrojs/sitemap": "^3.2.1",
    "@fontsource-variable/inter": "^5.1.1",
    "astro": "^5.1.1",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/nodemailer": "^8.0.1",
    "linkedom": "^0.18.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
